apiVersion: v1
kind: ServiceAccount
metadata:
  name: loan-prediction-sa
  namespace: loan-prediction-mlops
  annotations:
    # AWS IAM role for S3 access (IRSA - IAM Roles for Service Accounts)
    # Replace with your actual IAM role ARN that has S3 access permissions
    # Example: arn:aws:iam::123456789012:role/loan-prediction-s3-access
    eks.amazonaws.com/role-arn: arn:aws:iam::078607863580:role/loan-prediction-s3-access